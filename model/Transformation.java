package model;

/**
 * This enum represents different types of color transformation operations that could be
 * performed on an object of an image model.
 */
public enum Transformation {
  RED, GREEN, BLUE, MAXVALUE, LUMA, INTENSITY, SEPIA
}
